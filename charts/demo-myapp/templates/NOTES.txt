1. Get the service URL:
   kubectl get svc -n {{ .Release.Namespace }} {{ include "demo-myapp.fullname" . }}

2. If using Ingress, ensure your DNS points to the ingress controller and that
   values.ingress.enabled=true and hosts/tls are set correctly.

3. Image in use:
   {{ .Values.image.repository }}:{{ .Values.image.tag }}