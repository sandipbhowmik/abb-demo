apiVersion: v1
kind: Service
metadata:
  name: {{ include "demo-myapp.fullname" . }}
  labels:
    {{- include "demo-myapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "demo-myapp.selectorLabels" . | nindent 4 }}
